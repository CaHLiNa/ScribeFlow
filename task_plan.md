# 跟进问题修复计划

日期：2026-03-19

## 目标

完成以下 4 个问题的定位、修复、验证，并同步记录实现边界：

1. LaTeX 中文路径问题
2. 自动保存改成开关；关闭修改过的文件或新文件时弹出保存确认
3. 文件版本历史未实装
4. `@` 文件搜索不全

## 阶段

| 阶段 | 状态 | 内容 |
| --- | --- | --- |
| Phase 1 | completed | 代码定位与问题边界确认 |
| Phase 2 | completed | LaTeX 中文路径问题修复 |
| Phase 3 | completed | 关闭前保存确认与自动保存开关整合 |
| Phase 4 | completed | 文件版本历史补实装或接通现有能力 |
| Phase 5 | completed | `@` 文件搜索范围与匹配逻辑修复 |
| Phase 6 | completed | 构建验证与文档回写 |

## 风险

- 当前工作区已经有未提交改动，修复时不能回退已有更改。
- “文件版本历史”可能涉及前后端双端联动，需要先确认现有存储与 UI 是否已有半成品。
- LaTeX 中文路径问题可能在 Rust 命令侧、编译参数侧、或预览路径映射侧。

## 验证

- 前端：`npm run build`
- Rust：`cargo check --manifest-path src-tauri/Cargo.toml`
- 必要时补充针对具体功能的手工验证说明

## 执行结果

- 已新增 `Auto Save` 开关，并补齐文件脏状态、关闭 tab / pane / workspace 前的保存确认弹窗。
- 已为版本历史补上 `git init` 能力；普通工作区首次使用“版本历史 / 保存快照”时会自动初始化本地仓库。
- 已重写 `@` 文件搜索的结果组织，合并工作区索引、当前打开文件、最近文件，并按命中质量排序。
- 已在 LaTeX 相关外部命令上统一注入 UTF-8 locale，并改为优先使用源码目录下的相对文件参数，降低中文路径环境差异带来的失败概率。
